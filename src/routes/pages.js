import { Hono } from 'hono';
import { html } from 'hono/html';
import { getCookie, setCookie } from 'hono/cookie';
import { randomBytes } from 'node:crypto';
import { layout } from '../templates/layout.js';
import { CSRF_COOKIE } from '../services/authService.js';
import { requireAuth } from '../middleware/auth.js';
import { query } from '../db/postgres.js';
import { env } from '../config/env.js';

export const pages = new Hono();

function csrfToken(c) {
  const existing = getCookie(c, CSRF_COOKIE);
  if (existing) return existing;
  const token = randomBytes(32).toString('base64url');
  setCookie(c, CSRF_COOKIE, token, { httpOnly: false, secure: c.req.url.startsWith('https:'), sameSite: 'Lax', path: '/', maxAge: 60 * 60 * 24 * 7 });
  return token;
}

function escapeHtml(value = '') { return String(value).replace(/[&<>\"]/g, (ch) => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '\"': '&quot;' }[ch])); }

function render(c, title, children) {
  return c.html(html`${layout({ title, children, user: c.get('user') })}`);
}

pages.get('/', (c) => render(c, 'Paid API proxy platform', `
<section class="hero text-white"><div class="container py-5"><div class="row align-items-center g-5 py-4"><div class="col-lg-7"><span class="badge text-bg-primary mb-3">Bun + Hono + PostgreSQL + Redis</span><h1 class="display-4 fw-bold">Sell one central API that proxies every upstream service.</h1><p class="lead text-white-50 mt-3">QuickRest combines backend APIs behind a single gateway, validates customers, tracks credits, and gives SaaS teams a clean place to meter usage.</p><div class="d-flex gap-3 mt-4 flex-wrap"><a href="/dashboard" class="btn btn-primary btn-lg">Open dashboard</a><a href="/api/proxy/httpbin?hello=quickrest" class="btn btn-outline-light btn-lg">Try demo proxy</a></div></div><div class="col-lg-5"><div class="card shadow-lg border-0 api-card"><div class="card-body p-4"><p class="text-uppercase text-muted small mb-2">Gateway request</p><pre class="mb-0"><code>GET /api/proxy/weather\nAuthorization: Bearer qrst_...\nX-Credits-Cost: 1</code></pre></div></div></div></div></div></section>
<section class="container py-5"><div class="row g-4">${['API aggregation', 'Credit billing', 'Rate limiting'].map((title) => `<div class="col-md-4"><div class="card h-100 shadow-sm"><div class="card-body"><h2 class="h5">${title}</h2><p class="text-muted mb-0">Configure upstream services, control customer access, and protect infrastructure with Redis-backed limits.</p></div></div></div>`).join('')}</div></section>`));

pages.get('/signup', (c) => render(c, 'Sign up', `
<div class="container py-5 auth-page"><div class="row justify-content-center"><div class="col-lg-6"><div class="card shadow-sm"><div class="card-body p-4"><h1 class="h3 mb-3">Create your account</h1><form class="needs-validation" method="post" action="/auth/signup" novalidate><input type="hidden" name="csrfToken" value="${csrfToken(c)}"><div class="mb-3"><label class="form-label">Username</label><input class="form-control" name="username" minlength="3" maxlength="64" pattern="[A-Za-z0-9_-]+" data-check-username required><div class="form-text" data-username-feedback>Use 3-64 letters, numbers, underscores, or hyphens.</div></div><div class="mb-3"><label class="form-label">Email</label><input class="form-control" type="email" name="email" data-check-email required><div class="form-text" data-email-feedback>We will verify this email is unique.</div></div><div class="mb-3"><label class="form-label">Password</label><input class="form-control" type="password" name="password" minlength="8" required><div class="invalid-feedback">Use at least 8 characters.</div></div><div class="mb-3"><label class="form-label">Recovery PIN</label><input class="form-control" name="recoveryPin" inputmode="numeric" pattern="\\d{4,12}" required><div class="form-text">Recovery PINs are hashed like passwords. Store yours somewhere safe; support cannot read it.</div></div><button class="btn btn-primary w-100">Sign up</button></form></div></div></div></div></div>`));

pages.get('/login', (c) => render(c, 'Login', `
<div class="container py-5 auth-page"><div class="row justify-content-center"><div class="col-lg-5"><div class="card shadow-sm"><div class="card-body p-4"><h1 class="h3 mb-3">Login</h1>${c.req.query('error') ? `<div class="alert alert-danger">${c.req.query('error')}</div>` : ''}<form class="needs-validation" method="post" action="/auth/login" novalidate><input type="hidden" name="csrfToken" value="${csrfToken(c)}"><div class="mb-3"><label class="form-label">Email</label><input class="form-control" type="email" name="email" required></div><div class="mb-3"><label class="form-label">Password</label><input class="form-control" type="password" name="password" minlength="8" required></div><button class="btn btn-primary w-100">Login</button></form><p class="mt-3 mb-0 text-center"><a href="/forgot-password">Forgot password?</a></p></div></div></div></div></div>`));


pages.get('/forgot-password', (c) => render(c, 'Forgot password', `
<div class="container py-5 auth-page"><div class="row justify-content-center"><div class="col-lg-5"><div class="card shadow-sm"><div class="card-body p-4"><h1 class="h3 mb-3">Forgot password</h1>${c.req.query('error') ? `<div class="alert alert-danger">${escapeHtml(c.req.query('error'))}</div>` : ''}<p class="text-muted">Enter your email address or username, then verify your recovery PIN.</p><form method="post" action="/auth/password-recovery/request"><input type="hidden" name="csrfToken" value="${csrfToken(c)}"><div class="mb-3"><label class="form-label">Email or username</label><input class="form-control" name="identity" required autocomplete="username"></div><button class="btn btn-primary w-100">Continue</button></form><p class="mt-3 mb-0"><a href="/login">Back to login</a></p></div></div></div></div></div>`));

pages.get('/verify-recovery-pin', (c) => render(c, 'Verify recovery PIN', `
<div class="container py-5 auth-page"><div class="row justify-content-center"><div class="col-lg-5"><div class="card shadow-sm"><div class="card-body p-4"><h1 class="h3 mb-3">Verify recovery PIN</h1>${c.req.query('error') ? `<div class="alert alert-danger">${escapeHtml(c.req.query('error'))}</div>` : ''}<form method="post" action="/auth/password-recovery/verify-pin"><input type="hidden" name="csrfToken" value="${csrfToken(c)}"><div class="mb-3"><label class="form-label">Email or username</label><input class="form-control" name="identity" value="${escapeHtml(c.req.query('identity') || '')}" required autocomplete="username"></div><div class="mb-3"><label class="form-label">Recovery PIN</label><input class="form-control" name="recoveryPin" inputmode="numeric" pattern="\d{4,12}" required autocomplete="one-time-code"><div class="form-text">Too many incorrect attempts temporarily locks recovery.</div></div><button class="btn btn-primary w-100">Verify PIN</button></form></div></div></div></div></div>`));

pages.get('/set-new-password', (c) => render(c, 'Set new password', `
<div class="container py-5 auth-page"><div class="row justify-content-center"><div class="col-lg-5"><div class="card shadow-sm"><div class="card-body p-4"><h1 class="h3 mb-3">Set new password</h1>${c.req.query('error') ? `<div class="alert alert-danger">${escapeHtml(c.req.query('error'))}</div>` : ''}<form method="post" action="/auth/password-recovery/reset"><input type="hidden" name="csrfToken" value="${csrfToken(c)}"><input type="hidden" name="resetSession" value="${escapeHtml(c.req.query('resetSession') || '')}"><div class="mb-3"><label class="form-label">New password</label><input class="form-control" type="password" name="password" minlength="8" required autocomplete="new-password"><div class="invalid-feedback">Use at least 8 characters.</div></div><button class="btn btn-primary w-100">Reset password</button></form></div></div></div></div></div>`));

pages.get('/recovery-success', (c) => render(c, 'Recovery success', `
<div class="container py-5 auth-page"><div class="row justify-content-center"><div class="col-lg-5"><div class="card shadow-sm"><div class="card-body p-4 text-center"><h1 class="h3 mb-3">Password reset</h1><p class="text-muted">Your password was reset successfully. You can now log in with the new password.</p><a class="btn btn-primary" href="/login">Go to login</a></div></div></div></div></div>`));

pages.get('/logout', requireAuth, (c) => render(c, 'Logout', `<div class="container py-5"><div class="card shadow-sm"><div class="card-body"><h1 class="h3">Logout</h1><p>End your current session?</p><form method="post" action="/auth/logout"><input type="hidden" name="csrfToken" value="${csrfToken(c)}"><button class="btn btn-danger">Logout</button></form></div></div></div>`));

pages.get('/account', requireAuth, (c) => render(c, 'Account settings', `<div class="container py-5"><h1 class="fw-bold">Account settings</h1><div class="card shadow-sm"><div class="card-body"><dl class="row mb-0"><dt class="col-sm-3">Username</dt><dd class="col-sm-9">${escapeHtml(c.get('user').username)}</dd><dt class="col-sm-3">Email</dt><dd class="col-sm-9">${escapeHtml(c.get('user').email)}</dd><dt class="col-sm-3">Role</dt><dd class="col-sm-9"><span class="badge text-bg-secondary">${escapeHtml(c.get('user').role)}</span></dd><dt class="col-sm-3">Status</dt><dd class="col-sm-9">${escapeHtml(c.get('user').status)}</dd></dl></div></div></div>`));


function endpointDescription(endpoint) {
  return endpoint.description || `Send authenticated ${endpoint.http_method} requests through the QuickRest gateway.`;
}

function requiredHeaders(endpoint) {
  const configured = Array.isArray(endpoint.headers_config?.forwardHeaders) ? endpoint.headers_config.forwardHeaders : [];
  return ['Authorization: Bearer <api_key>', ...configured.filter((name) => !['authorization', 'x-api-key', 'cookie'].includes(String(name).toLowerCase())).map((name) => `${name}: <value>`)];
}

function examplePayload(method) {
  return ['POST', 'PUT', 'PATCH'].includes(method) ? ` \\\n  -H "Content-Type: application/json" \\\n  -d '{"example":"value"}'` : '';
}

function endpointDocsCard(endpoint, apiBaseUrl, apiKey) {
  const url = `${apiBaseUrl}${endpoint.public_path}`;
  const request = `curl -X ${endpoint.http_method} "${url}" \\\n  -H "Authorization: Bearer ${apiKey}"${examplePayload(endpoint.http_method)}`;
  const response = `{
  "status": "proxied response placeholder",
  "requestId": "req_example",
  "data": {}
}`;
  const headers = requiredHeaders(endpoint).map((header) => `<span class="badge text-bg-light border me-1 mb-1">${escapeHtml(header)}</span>`).join('');
  return `<div class="card docs-endpoint-card shadow-sm mb-4"><div class="card-body p-4"><div class="d-flex justify-content-between align-items-start gap-3 flex-wrap"><div><span class="badge text-bg-primary me-2">${escapeHtml(endpoint.http_method)}</span><code>${escapeHtml(endpoint.public_path)}</code><h2 class="h5 mt-3 mb-2">${escapeHtml(endpointDescription(endpoint))}</h2></div><div class="text-end"><div class="small text-muted">Credit cost</div><div class="display-6 fw-bold">${Number(endpoint.credit_cost).toLocaleString()}</div></div></div><hr><div class="row g-4"><div class="col-lg-6"><h3 class="h6">Required headers</h3><div class="mb-3">${headers}</div><h3 class="h6">Example request</h3><div class="code-snippet"><button class="btn btn-sm btn-outline-light" data-copy-target="request-${endpoint.id}">Copy</button><pre><code id="request-${endpoint.id}">${escapeHtml(request)}</code></pre></div></div><div class="col-lg-6"><h3 class="h6">Example response placeholder</h3><div class="code-snippet"><button class="btn btn-sm btn-outline-light" data-copy-target="response-${endpoint.id}">Copy</button><pre><code id="response-${endpoint.id}">${escapeHtml(response)}</code></pre></div><p class="small text-muted mt-3 mb-0">Backend target URLs are intentionally hidden. QuickRest proxies this public path to the configured upstream service.</p></div></div></div></div>`;
}

pages.get('/docs', (c) => render(c, 'API documentation', `
<section class="docs-hero text-white"><div class="container py-5"><div class="row align-items-center g-4"><div class="col-lg-8"><span class="badge text-bg-light text-primary mb-3">Public documentation</span><h1 class="display-5 fw-bold">One documented gateway for every paid API.</h1><p class="lead text-white-50">QuickRest provides a credit-metered API proxy, API-key authentication, domain controls, rate limits, billing, and usage analytics without exposing upstream backend URLs to customers.</p><div class="d-flex gap-3 flex-wrap mt-4"><a class="btn btn-light btn-lg" href="/docs/endpoints">View authenticated endpoint docs</a><a class="btn btn-outline-light btn-lg" href="/signup">Create account</a></div></div><div class="col-lg-4"><div class="card bg-dark border-primary"><div class="card-body"><p class="text-uppercase small text-primary-emphasis mb-2">Base URL</p><code class="text-info">${escapeHtml(env.APP_URL.replace(/\/$/, ''))}</code><hr class="border-secondary"><p class="small text-white-50 mb-0">Use your public paths with an API key. The upstream target remains private.</p></div></div></div></div></div></section>
<section class="container py-5"><div class="row g-4"><div class="col-md-4"><div class="card h-100 shadow-sm"><div class="card-body"><h2 class="h5">Authentication</h2><p class="text-muted mb-0">Send <code>Authorization: Bearer qrst_...</code> or <code>X-API-Key</code> with every endpoint request.</p></div></div></div><div class="col-md-4"><div class="card h-100 shadow-sm"><div class="card-body"><h2 class="h5">Credits</h2><p class="text-muted mb-0">Each endpoint declares its credit cost. Successful proxy calls deduct credits from your account balance.</p></div></div></div><div class="col-md-4"><div class="card h-100 shadow-sm"><div class="card-body"><h2 class="h5">Protected upstreams</h2><p class="text-muted mb-0">Documentation displays only public gateway paths, methods, request headers, and examples—not target backend URLs.</p></div></div></div></div></section>`));

pages.get('/docs/endpoints', requireAuth, async (c) => {
  const endpoints = await query(`select id, public_path, http_method, headers_config, credit_cost, description from proxy_endpoints where is_enabled = true order by public_path, http_method`);
  const keys = await query('select key_prefix from api_keys where user_id = $1 and status = $2 order by created_at desc limit 1', [c.get('user').id, 'active']);
  const apiKey = keys.rows[0]?.key_prefix ? `${keys.rows[0].key_prefix}...` : 'qrst_your_api_key';
  const apiBaseUrl = env.APP_URL.replace(/\/$/, '');
  const cards = endpoints.rows.map((endpoint) => endpointDocsCard(endpoint, apiBaseUrl, apiKey)).join('') || '<div class="alert alert-info">No enabled endpoints are available yet.</div>';
  return render(c, 'Authenticated API docs', `<div class="docs-shell"><div class="container py-5"><div class="d-flex justify-content-between align-items-start gap-3 flex-wrap mb-4"><div><span class="badge text-bg-primary mb-2">Authenticated documentation</span><h1 class="fw-bold">Available endpoints</h1><p class="text-muted mb-0">These are the public gateway paths available to your account. Target backend URLs are never shown.</p></div><a class="btn btn-outline-primary" href="/api-key">Manage API key</a></div>${nav()}<div class="alert alert-secondary"><strong>Base URL:</strong> <code>${escapeHtml(apiBaseUrl)}</code> · Include your API key in every request.</div>${cards}</div></div>`);
});

function nav() { return `<div class="list-group mb-4"><a class="list-group-item" href="/dashboard">Customer dashboard</a><a class="list-group-item" href="/docs/endpoints">API docs</a><a class="list-group-item" href="/api-key">API key</a><a class="list-group-item" href="/domains">Allowed domains</a><a class="list-group-item" href="/credits">Credits & billing</a><a class="list-group-item" href="/usage">Usage logs</a><a class="list-group-item" href="/account">Account settings</a></div>`; }

pages.get('/dashboard', requireAuth, async (c) => {
  const userId = c.get('user').id;
  const [balance, today, topEndpoints, recent, failures, domains, keys, txs, sessions, dailyUsage] = await Promise.all([
    query('select balance, lifetime_purchased, lifetime_used from credit_balances where user_id = $1 and currency = $2', [userId, 'credits']),
    query(`select count(*)::int requests_today from api_usage_logs where user_id = $1 and created_at >= date_trunc('day', now())`, [userId]),
    query(`select coalesce(e.public_path, l.service_slug, l.request_path) endpoint, count(*)::int calls, coalesce(sum(l.credits_charged),0)::int credits
             from api_usage_logs l left join proxy_endpoints e on e.id = l.endpoint_id
            where l.user_id = $1 group by 1 order by calls desc limit 5`, [userId]),
    query(`select l.created_at, coalesce(e.public_path, l.service_slug, l.request_path) endpoint, l.request_method, l.request_path, l.response_status, l.credits_charged, l.duration_ms, l.failure_reason
             from api_usage_logs l left join proxy_endpoints e on e.id = l.endpoint_id
            where l.user_id = $1 order by l.created_at desc limit 8`, [userId]),
    query(`select l.created_at, coalesce(e.public_path, l.service_slug, l.request_path) endpoint, l.request_method, l.response_status, l.failure_reason, l.request_domain
             from api_usage_logs l left join proxy_endpoints e on e.id = l.endpoint_id
            where l.user_id = $1 and (l.auth_success = false or coalesce(l.response_status, 0) >= 400) order by l.created_at desc limit 10`, [userId]),
    query('select domain, status from allowed_domains where user_id = $1 order by created_at desc limit 6', [userId]),
    query('select name, key_prefix, status, last_used_at from api_keys where user_id = $1 order by created_at desc limit 3', [userId]),
    query(`select transaction_type, amount, balance_after, description, created_at from credit_transactions where user_id = $1 order by created_at desc limit 6`, [userId]),
    query(`select s.created_at, p.name, s.credits, s.amount_total_cents, s.currency, s.status, s.payment_status
             from stripe_checkout_sessions s left join credit_packages p on p.id = s.credit_package_id
            where s.user_id = $1 order by s.created_at desc limit 5`, [userId]),
    query(`select to_char(day, 'Dy') label, coalesce(count(l.id), 0)::int calls, coalesce(sum(l.credits_charged), 0)::int credits
             from generate_series(date_trunc('day', now()) - interval '6 days', date_trunc('day', now()), interval '1 day') day
             left join api_usage_logs l on l.user_id = $1 and l.created_at >= day and l.created_at < day + interval '1 day'
            group by day order by day`, [userId])
  ]);
  const b = balance.rows[0] || { balance: 0, lifetime_purchased: 0, lifetime_used: 0 };
  const maxCalls = Math.max(1, ...dailyUsage.rows.map((r) => r.calls));
  const usageBars = dailyUsage.rows.map((r) => `<div class="chart-column"><div class="chart-bar" style="height:${Math.max(8, Math.round((r.calls / maxCalls) * 100))}%"></div><span>${escapeHtml(r.label)}</span></div>`).join('');
  const endpointRows = topEndpoints.rows.map(r => `<tr><td>${escapeHtml(r.endpoint || '')}</td><td class="text-end">${r.calls}</td><td class="text-end">${r.credits}</td></tr>`).join('') || '<tr><td colspan="3" class="text-muted">No usage yet.</td></tr>';
  const recentRows = recent.rows.map(r => `<tr><td>${r.created_at}</td><td>${escapeHtml(r.request_method)} ${escapeHtml(r.request_path)}</td><td><span class="badge rounded-pill ${Number(r.response_status) >= 400 ? 'text-bg-danger' : 'text-bg-success'}">${r.response_status ?? '—'}</span></td><td>${r.credits_charged}</td><td>${r.duration_ms ?? '—'} ms</td></tr>`).join('') || '<tr><td colspan="5" class="text-muted">No recent requests.</td></tr>';
  const domainRows = domains.rows.map(d => `<span class="badge rounded-pill ${d.status === 'active' ? 'text-bg-primary' : 'text-bg-secondary'} me-1 mb-1">${escapeHtml(d.domain)}</span>`).join('') || '<span class="text-muted">No domains configured.</span>';
  const keyRows = keys.rows.map(k => `<div class="d-flex justify-content-between align-items-center py-2 border-bottom"><div><div class="fw-semibold">${escapeHtml(k.name)}</div><code>${escapeHtml(k.key_prefix)}…</code></div><span class="badge text-bg-light border">${escapeHtml(k.status)}</span></div>`).join('') || '<p class="text-muted mb-0">No API keys yet.</p>';
  const txRows = txs.rows.map(t => `<tr><td>${t.created_at}</td><td>${escapeHtml(t.transaction_type)}</td><td>${t.amount}</td><td>${t.balance_after ?? ''}</td><td>${escapeHtml(t.description || '')}</td></tr>`).join('') || '<tr><td colspan="5" class="text-muted">No credit transactions yet.</td></tr>';
  const billingRows = sessions.rows.map(s => `<tr><td>${s.created_at}</td><td>${escapeHtml(s.name || 'Credit package')}</td><td>${s.credits}</td><td>$${(s.amount_total_cents / 100).toFixed(2)} ${escapeHtml(s.currency.toUpperCase())}</td><td>${escapeHtml(s.payment_status || s.status)}</td></tr>`).join('') || '<tr><td colspan="5" class="text-muted">No billing history yet.</td></tr>';
  const apiBase = `${env.APP_URL.replace(/\/$/, '')}/api/proxy/httpbin`;
  const apiKey = keys.rows[0]?.key_prefix ? `${keys.rows[0].key_prefix}...` : 'qrst_your_api_key';
  const jsExample = `fetch('${apiBase}?hello=quickrest', {\n  headers: { Authorization: 'Bearer ${apiKey}' }\n}).then((res) => res.json())`;
  const nodeExample = `const response = await fetch('${apiBase}?hello=quickrest', {\n  headers: { Authorization: 'Bearer ${apiKey}' }\n});\nconsole.log(await response.json());`;
  return render(c, 'Customer dashboard', `<div class="dashboard-shell"><div class="container py-5"><div class="dashboard-hero card border-0 mb-4"><div class="card-body p-4 p-lg-5"><div class="row align-items-center g-4"><div class="col-lg-8"><span class="badge text-bg-primary mb-3">Customer workspace</span><h1 class="display-6 fw-bold mb-2">Build, monitor, and bill your API traffic.</h1><p class="lead text-white-50 mb-0">Manage credits, keys, domains, usage analytics, billing, and proxy API documentation from one responsive SaaS dashboard.</p></div><div class="col-lg-4 text-lg-end"><a class="btn btn-light btn-lg me-2 mb-2" href="/credits">Buy credits</a><a class="btn btn-outline-light btn-lg mb-2" href="#api-docs">View docs</a></div></div></div></div>${nav()}<div class="row g-4 mb-4"><div class="col-lg-4"><div class="card metric-card credit-card h-100"><div class="card-body"><div class="d-flex justify-content-between"><span class="text-muted">Credit balance</span><span class="badge text-bg-success">Live</span></div><h2>${Number(b.balance).toLocaleString()}</h2><p class="small text-muted">Purchased ${Number(b.lifetime_purchased).toLocaleString()} · Used ${Number(b.lifetime_used).toLocaleString()}</p><a class="btn btn-primary w-100" href="/credits">Buy credits</a></div></div></div><div class="col-md-6 col-lg-4"><div class="card metric-card h-100"><div class="card-body"><span class="text-muted">Requests today</span><h2>${today.rows[0].requests_today}</h2><p class="small text-muted mb-0">Across all API keys and domains.</p></div></div></div><div class="col-md-6 col-lg-4"><div class="card metric-card h-100"><div class="card-body"><span class="text-muted">Needs attention</span><h2>${failures.rows.length}</h2><p class="small text-muted mb-0">Recent auth or upstream failures.</p></div></div></div></div><div class="row g-4 mb-4"><div class="col-xl-8"><div class="card h-100"><div class="card-body"><div class="d-flex justify-content-between align-items-center mb-3"><h2 class="h5 mb-0">Usage charts</h2><span class="text-muted small">Last 7 days</span></div><div class="mini-chart">${usageBars}</div><h3 class="h6 mt-4">Most used endpoints</h3><div class="table-responsive"><table class="table table-sm align-middle"><thead><tr><th>Endpoint</th><th class="text-end">Calls</th><th class="text-end">Credits</th></tr></thead><tbody>${endpointRows}</tbody></table></div></div></div></div><div class="col-xl-4"><div class="card h-100"><div class="card-body"><h2 class="h5">API key management</h2>${keyRows}<form method="post" action="/customer/api-key/rotate" class="mt-3"><input type="hidden" name="csrfToken" value="${csrfToken(c)}"><button class="btn btn-outline-warning w-100">Rotate API key</button></form><hr><h2 class="h5">Allowed domains</h2><div class="mb-3">${domainRows}</div><a class="btn btn-outline-primary w-100" href="/domains">Manage domains</a></div></div></div></div><div class="row g-4 mb-4"><div class="col-xl-7"><div class="card h-100"><div class="card-body"><div class="d-flex justify-content-between align-items-center"><h2 class="h5">Recent API calls</h2><a href="/usage" class="small">View all</a></div><div class="table-responsive"><table class="table table-hover align-middle"><thead><tr><th>Date</th><th>Request</th><th>Status</th><th>Credits</th><th>Latency</th></tr></thead><tbody>${recentRows}</tbody></table></div></div></div></div><div class="col-xl-5"><div class="card h-100"><div class="card-body"><h2 class="h5">Account settings</h2><dl class="row small mb-3"><dt class="col-4">Username</dt><dd class="col-8">${escapeHtml(c.get('user').username)}</dd><dt class="col-4">Email</dt><dd class="col-8">${escapeHtml(c.get('user').email)}</dd><dt class="col-4">Role</dt><dd class="col-8"><span class="badge text-bg-secondary">${escapeHtml(c.get('user').role)}</span></dd></dl><a class="btn btn-outline-secondary w-100" href="/account">Open account settings</a></div></div></div></div><div class="row g-4 mb-4"><div class="col-lg-6"><div class="card h-100"><div class="card-body"><h2 class="h5">Credit transaction history</h2><div class="table-responsive"><table class="table table-sm"><thead><tr><th>Date</th><th>Type</th><th>Amount</th><th>Balance</th><th>Description</th></tr></thead><tbody>${txRows}</tbody></table></div></div></div></div><div class="col-lg-6"><div class="card h-100"><div class="card-body"><h2 class="h5">Billing history</h2><div class="table-responsive"><table class="table table-sm"><thead><tr><th>Date</th><th>Package</th><th>Credits</th><th>Total</th><th>Payment</th></tr></thead><tbody>${billingRows}</tbody></table></div></div></div></div></div><div id="api-docs" class="card api-card border-0 shadow-sm"><div class="card-body p-4"><div class="d-flex justify-content-between flex-wrap gap-2"><div><h2 class="h4">Proxy API documentation</h2><p class="text-muted">Call any configured proxy endpoint with your API key in the Authorization header. Domain allowlisting is enforced for browser requests.</p></div><code class="align-self-start">GET /api/proxy/:service</code></div><div class="row g-3"><div class="col-lg-4"><h3 class="h6">curl</h3><pre><code>curl -H "Authorization: Bearer ${apiKey}" \\\n  "${apiBase}?hello=quickrest"</code></pre></div><div class="col-lg-4"><h3 class="h6">JavaScript fetch</h3><pre><code>${escapeHtml(jsExample)}</code></pre></div><div class="col-lg-4"><h3 class="h6">Node/Bun</h3><pre><code>${escapeHtml(nodeExample)}</code></pre></div></div></div></div></div></div>`);
});

pages.get('/api-key', requireAuth, async (c) => {
  const keys = await query('select id, name, key_prefix, status, last_used_at, created_at from api_keys where user_id = $1 order by created_at desc', [c.get('user').id]);
  const rows = keys.rows.map(k => `<tr><td>${escapeHtml(k.name)}</td><td><code>${escapeHtml(k.key_prefix)}…</code></td><td>${escapeHtml(k.status)}</td><td>${k.last_used_at || 'Never'}</td><td>${k.created_at}</td></tr>`).join('');
  return render(c, 'API key', `<div class="container py-5"><h1 class="fw-bold">API key</h1>${nav()}<div class="alert alert-info">Raw API keys are shown only once when generated. Rotate to receive a new key.</div><form method="post" action="/customer/api-key/rotate"><input type="hidden" name="csrfToken" value="${csrfToken(c)}"><button class="btn btn-warning mb-3">Rotate API key</button></form><div class="table-responsive"><table class="table"><thead><tr><th>Name</th><th>Prefix</th><th>Status</th><th>Last used</th><th>Created</th></tr></thead><tbody>${rows}</tbody></table></div></div>`);
});

pages.get('/domains', requireAuth, async (c) => {
  const user = (await query('select wildcard_domains_enabled from users where id = $1', [c.get('user').id])).rows[0];
  const domains = await query('select id, domain, status, created_at from allowed_domains where user_id = $1 order by created_at desc', [c.get('user').id]);
  const rows = domains.rows.map(d => `<tr><form method="post" action="/customer/domains/${d.id}"><td><input class="form-control" name="domain" value="${escapeHtml(d.domain)}"></td><td><select class="form-select" name="status"><option ${d.status==='active'?'selected':''}>active</option><option ${d.status==='disabled'?'selected':''}>disabled</option></select></td><td><input type="hidden" name="csrfToken" value="${csrfToken(c)}"><button class="btn btn-sm btn-primary">Save</button></form><form class="d-inline" method="post" action="/customer/domains/${d.id}/delete"><input type="hidden" name="csrfToken" value="${csrfToken(c)}"><button class="btn btn-sm btn-outline-danger">Delete</button></form></td></tr>`).join('');
  return render(c, 'Allowed domains', `<div class="container py-5"><h1 class="fw-bold">Allowed domains</h1>${nav()}<form method="post" action="/customer/domains/wildcard" class="form-check form-switch mb-3"><input type="hidden" name="csrfToken" value="${csrfToken(c)}"><input class="form-check-input" type="checkbox" name="enabled" onchange="this.form.submit()" ${user?.wildcard_domains_enabled?'checked':''}><label class="form-check-label">Allow wildcard access from any domain</label></form><form class="row g-2 mb-4" method="post" action="/customer/domains"><input type="hidden" name="csrfToken" value="${csrfToken(c)}"><div class="col"><input class="form-control" name="domain" placeholder="example.com or *.example.com" required></div><div class="col-auto"><button class="btn btn-primary">Add domain</button></div></form><table class="table"><thead><tr><th>Domain</th><th>Status</th><th>Actions</th></tr></thead><tbody>${rows}</tbody></table></div>`);
});

pages.get('/credits', requireAuth, async (c) => {
  const userId = c.get('user').id;
  const [balance, txs, packages, sessions] = await Promise.all([
    query('select balance, lifetime_purchased, lifetime_used from credit_balances where user_id = $1', [userId]),
    query(`select transaction_type, amount, balance_after, stripe_reference, request_id, description, created_at from credit_transactions where user_id = $1 order by created_at desc limit 100`, [userId]),
    query('select id, name, credits, amount_cents, currency from credit_packages where is_active = true order by sort_order, amount_cents'),
    query(`select s.created_at, p.name, s.credits, s.amount_total_cents, s.currency, s.status, s.payment_status, s.stripe_session_id
             from stripe_checkout_sessions s left join credit_packages p on p.id = s.credit_package_id
            where s.user_id = $1 order by s.created_at desc limit 50`, [userId])
  ]);
  const b = balance.rows[0] || { balance: 0, lifetime_purchased: 0, lifetime_used: 0 };
  const packageCards = packages.rows.map(p => `<div class="col-md-4"><div class="card h-100 shadow-sm"><div class="card-body"><h3 class="h5">${escapeHtml(p.name)}</h3><p class="display-6 fw-bold">${p.credits.toLocaleString()}</p><p class="text-muted">credits for $${(p.amount_cents / 100).toFixed(2)} ${escapeHtml(p.currency.toUpperCase())}</p><form method="post" action="/billing/create-checkout-session"><input type="hidden" name="csrfToken" value="${csrfToken(c)}"><input type="hidden" name="packageId" value="${p.id}"><button class="btn btn-primary w-100">Buy credits</button></form></div></div></div>`).join('') || '<p class="text-muted">No credit packages are currently available.</p>';
  const purchaseRows = sessions.rows.map(s => `<tr><td>${s.created_at}</td><td>${escapeHtml(s.name || 'Credit package')}</td><td>${s.credits}</td><td>$${(s.amount_total_cents / 100).toFixed(2)} ${escapeHtml(s.currency.toUpperCase())}</td><td>${escapeHtml(s.status)}</td><td>${escapeHtml(s.payment_status)}</td><td><code>${escapeHtml(s.stripe_session_id)}</code></td></tr>`).join('');
  const rows = txs.rows.map(t => `<tr><td>${t.created_at}</td><td><span class="badge text-bg-secondary">${escapeHtml(t.transaction_type)}</span></td><td>${t.amount}</td><td>${t.balance_after ?? ''}</td><td>${escapeHtml(t.stripe_reference || t.request_id || '')}</td><td>${escapeHtml(t.description || '')}</td></tr>`).join('');
  return render(c, 'Credits', `<div class="container py-5"><h1 class="fw-bold">Credits & billing</h1>${nav()}<div class="card mb-4"><div class="card-body"><h2>${b.balance} credits</h2><p class="text-muted">Lifetime purchased: ${b.lifetime_purchased}. Lifetime used: ${b.lifetime_used}.</p></div></div><h2 class="h4">Buy credits</h2><div class="row g-4 mb-5">${packageCards}</div><h2 class="h5">Purchase history</h2><div class="table-responsive mb-5"><table class="table"><thead><tr><th>Date</th><th>Package</th><th>Credits</th><th>Amount</th><th>Status</th><th>Payment</th><th>Stripe session</th></tr></thead><tbody>${purchaseRows}</tbody></table></div><h2 class="h5">Credit history</h2><div class="table-responsive"><table class="table"><thead><tr><th>Date</th><th>Type</th><th>Amount</th><th>Balance after</th><th>Reference</th><th>Description</th></tr></thead><tbody>${rows}</tbody></table></div></div>`);
});

pages.get('/usage', requireAuth, async (c) => {
  const logs = await query('select service_slug, request_method, request_path, request_domain, response_status, credits_charged, auth_success, failure_reason, request_ip, duration_ms, created_at from api_usage_logs where user_id = $1 order by created_at desc limit 100', [c.get('user').id]);
  const rows = logs.rows.map(l => `<tr><td>${l.created_at}</td><td>${escapeHtml(l.service_slug || '')}</td><td>${escapeHtml(l.request_method)} ${escapeHtml(l.request_path)}</td><td>${escapeHtml(l.request_domain || '')}</td><td>${escapeHtml(l.request_ip || '')}</td><td>${l.response_status ?? ''}</td><td>${l.credits_charged}</td><td>${l.duration_ms ?? ''}</td><td>${l.auth_success ? 'OK' : escapeHtml(l.failure_reason || 'Failed')}</td></tr>`).join('');
  return render(c, 'Usage logs', `<div class="container py-5"><h1 class="fw-bold">Usage logs</h1>${nav()}<div class="table-responsive"><table class="table"><thead><tr><th>Date</th><th>Service</th><th>Request</th><th>Domain</th><th>IP</th><th>Status</th><th>Credits</th><th>Target ms</th><th>Auth</th></tr></thead><tbody>${rows}</tbody></table></div></div>`);
});
